320 llm calls had no session id,
98 evaluation entries had no llm call id , the telemetry recoder is not accurate if its the one generating all datasets, or mabe inconsistency in tools.

The billable total i believe what a user is actuallly expected to pas should be sucessfull calls.